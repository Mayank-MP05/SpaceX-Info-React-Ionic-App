import React from 'react';
interface LaunchProps {
  id: string;
}

const shipspage: React.FC<LaunchProps> = () => {

  return (
    <div className="container">
      <h1>Shpis List Rendering ...</h1>
    </div>
  );
};

export default shipspage;
